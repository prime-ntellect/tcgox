import { makeStyles } from '@material-ui/core/styles';

const styles = () =>
	makeStyles({
		root: {
			margin: '0 auto',
			width: '100%',
			maxWidth: '100%',
			padding: '0',
		},
	});

export default styles;
